import React from "react";
import WhisperComponent from "./WhisperComponent";

type Props = {};

export const Form = (props: Props) => {
  return (
    <div>
      <WhisperComponent />
    </div>
  );
};
